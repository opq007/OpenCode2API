# Copy this file to .env before running docker compose.

API_KEY=change-me
OPENCODE_SERVER_PASSWORD=change-me-too

# Safer default. Keep true unless you really want model tool access.
DISABLE_TOOLS=true

# External tool bridge is implemented by the proxy, not by OpenCode internal tools.
# Supported values today:
# - OPENCODE_EXTERNAL_TOOLS_MODE=proxy-bridge
# - OPENCODE_EXTERNAL_TOOLS_CONFLICT_POLICY=namespace
OPENCODE_EXTERNAL_TOOLS_MODE=proxy-bridge
OPENCODE_EXTERNAL_TOOLS_CONFLICT_POLICY=namespace

# Helpful when running behind OpenClaw or other clients that already manage prompts.
OPENCODE_PROXY_PROMPT_MODE=plugin-inject
OPENCODE_PROXY_OMIT_SYSTEM_PROMPT=true

# Periodically clean proxy-owned conversation/session storage.
OPENCODE_PROXY_AUTO_CLEANUP_CONVERSATIONS=true
OPENCODE_PROXY_CLEANUP_INTERVAL_MS=43200000
OPENCODE_PROXY_CLEANUP_MAX_AGE_MS=86400000

# Optional overrides
# OPENCODE_PROXY_PORT=10000
# OPENCODE_SERVER_PORT=10001
# OPENCODE_PROXY_REQUEST_TIMEOUT_MS=180000
# OPENCODE_PROXY_DEBUG=false
# OPENCODE_ZEN_API_KEY=

# Backend isolation ("client is the agent").
# The spawned opencode backend is sandboxed so the operator's local opencode
# environment (instructions, skills, agents, plugins, MCP servers, AGENTS.md)
# never influences client requests. Provider/model definitions are carried over;
# default keep-auth also copies the real auth.json so locally logged-in models
# keep working.
#   keep-auth - sandbox + copy the real auth.json (default)
#   full      - sandbox only; credentials via env vars
#   none      - use the real user home (debug only)
# OPENCODE_ISOLATION=keep-auth
# OPENCODE_JAIL_INLINE_KEYS=false  # set true to keep inline provider apiKey in the jail config (single-user hosts)

# Auto-start the OpenCode backend (opencode serve) when it is not reachable.
# Enabled by default. Set to false to manage the backend externally.
# OPENCODE_PROXY_MANAGE_BACKEND=true
